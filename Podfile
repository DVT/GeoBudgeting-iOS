# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GeoBudgeting' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # Pods for Drop Memory
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'Firebase'
  pod 'Firebase/Database'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'
#
 target 'GeoBudgetingTests' do
    inherit! :search_paths
#    # Pods for testing
  pod 'Firebase'
  end
target 'GeoBudgetingUITests' do
  inherit! :complete
  #    # Pods for testing
  pod 'Firebase'
end
end
